export enum EnumSizeProduct {
  S = "S",
  M = "M",
  L = "L",
}

export enum EnumTypeBtn {
  BUTTON = "button",
  SUBMIT = "submit",
  RESET = "reset",
}

export enum EnumCreaseUpDown {
  UP = "+",
  DOWN = "-",
}

export enum EnumTypeInput {
  TEXT = "text",
  PASSWORD = "password",
}
